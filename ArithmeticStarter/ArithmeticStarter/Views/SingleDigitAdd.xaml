<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ArithmeticStarter.Views.SingleDigitAdd">
    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <StackLayout HorizontalOptions="Fill" Orientation="Horizontal" >
            <Label Text="せいかいすう" FontSize="30" />
            <Label Text="{Binding CorrectCount}" FontSize="30" />
        </StackLayout>
        <StackLayout HorizontalOptions="CenterAndExpand" Orientation="Horizontal" >
            <Label Text="{Binding Left}" FontSize="50" />
            <Label Text="+" FontSize="50" />
            <Label Text="{Binding Right}" FontSize="50" />
        </StackLayout>
        <Entry Placeholder="" Keyboard="Numeric" MaxLength="100" FontSize="50" Text="{Binding Result}"/>
    </StackLayout>

</ContentPage>